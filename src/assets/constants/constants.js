const BASE_URL = `http://localhost:3000`;

const endpoints = {
  events: "events",
  users: "users",
  tickets: "tickets",
  categories: "categories",
  venues: "venues",
};

export { BASE_URL, endpoints };
